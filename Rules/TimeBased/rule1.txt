Input Parameters:[currenttime]
Event:{WITH "12:59 AM" AS currenttime}
Condition:{MATCH (r:Retailer)-[sb:Sells]->(p:Product)
WHERE p.name = p.name AND sb.ProductPresentQuantity > sb.ProductReqMinQuantity AND r.isStoreOpenforOrdering = "false"}
Action:{SET r.isStoreOpenforOrdering = CASE
WHEN r.openTime = currenttime
THEN "true" ELSE "false" END
RETURN r;}